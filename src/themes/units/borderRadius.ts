export const small = `${2 / 16}rem` as const;
export const medium = `${4 / 16}rem` as const;
export const larger = `${6 / 16}rem` as const;
export const large = `${8 / 16}rem` as const;
export const largest = `${16 / 16}rem` as const;
